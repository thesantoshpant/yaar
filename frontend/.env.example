# Backend base URL. Defaults to http://localhost:4000 if unset.
VITE_API_BASE=http://localhost:4000

# Google OAuth Web client ID (same one as the backend GOOGLE_CLIENT_ID).
# Without it, the app runs in guest mode and the sign-in button is hidden.
VITE_GOOGLE_CLIENT_ID=
